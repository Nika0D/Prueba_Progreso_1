#include <stdio.h>
/*Escriba un programa que reciba como datos de entrada n números 
enteros y regrese como dato de salida el mayor de estos números. 
Debe utilizar el concepto de función.*/
int main()
{
    int numero1;
    int continuar=1;
    while (continuar==1)
    {
        printf("ingrese un numero");
        scanf("%d", &numero1);
        int numero_m(res);
        printf("Si desea continuar escriba 1");
        scanf("%d", &continuar);
    }
    printf("El numero mayor es: %d\n", numero_m);
    return 0;
}
int numero_m(numero1){
    int numero1, res = 0, continuar=1;
    if (numero1 >= res)
    {
        res=numero1; 
    }
    return res;
}